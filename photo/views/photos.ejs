<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Express stock photo app</h1>
    <p>Hey there, heres some photos</p>
    <div id="photos">
    	<% photos.forEach(function(photo){ %>
    		<div class="photo">
	    		<h2><%=photo.name%></h2>
	    		<img src='<%=photo.path%>'/>
    		</div>
    	<%})%>
    </div>
  </body>
</html>